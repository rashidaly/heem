import 'package:flutter/material.dart';

class EventsClass extends StatelessWidget {
  const EventsClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Scaffold(
          body: Center(
        child: Text('Events'),
      ));
}
